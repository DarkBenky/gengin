# build outputs (all binaries, test/bench builds, pgo); prof charts are tracked
build/*
!build/prof/

# flamegraph tool clone
.flamegraph/

# profiling artifacts (legacy root locations)
perf.data
perf.data.old
perf_folded.txt
flamegraph.svg
callgraph.svg
*.profraw
*.profdata

# prof charts are tracked; raw perf data and the profiled binary stay ignored
!build/prof/*.svg
build/prof/*.perf.data
build/prof/main_flame

# bench results
bench/results/bench_results.json

# root binaries (legacy, now built under build/)
main
main_debug
main_bench
main_bench_unopt
main_pgo
exampleClient
exampleServer
gameClient
gameServer
hexDumpBin
flightController
flightController_debug
testSound
testSound3d
testRadarScreen

# llmOpt cloned workspace
llmOpt/gengin

# llmOpt project-scoped Hermes Agent state (config, sessions, logs)
llmOpt/.hermes/

# llmOpt supervisor runtime (state, session logs, per-session run artifacts)
llmOpt/state/
llmOpt/logs/
llmOpt/run/

# python caches + runtime baseline caches (both are machine-local, keyed by fingerprint)
__pycache__/
*.pyc
llmOpt/baseline_cache.json
llmOpt/flight_baseline.json

# large binary assets
assets/models/*.bin
assets/models/map.obj
assets/chars/*.bin

# generated images
tools/*.png
!tools/01.png
tests/img/

# trained model checkpoints
simulation/model.bin
simulation/trainNN
simulation/best_model_*.bin
simulation/best_backprop_model_*.bin

.ccls-cache
.cache/

simulation/wandb/
llmOpt/.env
llmOpt/.env.bak*

simulation/cSim/*.html
simulation/cSim/flightControlLogs.csv
simulation/cSim/flightDashboard.html
simulation/cSim/flightPath3d.html
